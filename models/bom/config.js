const SOURCE_FILE = `${__dirname}/../../source/BOM_PRODUCTION.xlsb`
const DATA_TAB = 'MASTER DATA'
const INVALID_STATUS = [] // ['P', 'K']

module.exports = {
    SOURCE_FILE,
    DATA_TAB,
    INVALID_STATUS
} 